[package]
name = "application"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = { workspace = true }
sea-orm = { workspace = true, features = ["macros", "with-time"] }
time = { workspace = true }
