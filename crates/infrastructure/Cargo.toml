[package]
name = "infrastructure"
version = "0.1.0"
edition = "2024"

[dependencies]
sea-orm-migration = { workspace = true, default-features = false, features = [
    "sqlx-postgres",
    "runtime-tokio",
] }
config = { workspace = true, features = ["toml"] }

# 序列化
serde = { workspace = true }